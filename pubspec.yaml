name: serialize_objects_type
description: A type-aware object serialization library for Dart with pluggable data codecs and storage backends.
version: 0.1.0
# repository: https://github.com/my_org/my_repo
publish_to: none
environment:
  sdk: ^3.12.1

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0
  json_annotation: ^4.12.0
  meta: ^1.18.3

dev_dependencies:
  build_runner: ^2.15.0
  lints: ^6.1.0
  test: ^1.31.1
  json_serializable: ^6.14.0
